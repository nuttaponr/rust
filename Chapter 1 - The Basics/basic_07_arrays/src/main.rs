fn main() {
    //ชนิดข้อมูลสำหรับ array คือ [T;N]
    let nums: [i32; 3] = [1, 2, 3]; //T คือ type ของสมาชิก และ N คือขนาดที่ถูกกำหนดให้รับรู้ตั้งแต่ compile-time
    println!("{:?}", nums);
    println!("{}", nums[1]); //การดึงข้อมูลแต่ละตัวออกมา ทำได้ด้วยการใช้ตัวดำเนินการ(operator) [x]
}
